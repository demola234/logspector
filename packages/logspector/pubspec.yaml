name: logspector
description: Rust-powered performance engine for Flutter, exposed via an in-app overlay and OpenTelemetry-compatible exporters.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
