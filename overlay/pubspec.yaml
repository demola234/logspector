name: logspector_overlay
description: In-app overlay HUD for logspector — Frames/Network/Logs/Memory tabs, shared timeline view.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logspector:
    path: ../packages/logspector

dev_dependencies:
  flutter_test:
    sdk: flutter
