name: basic_app
description: Example Flutter app demonstrating logspector integration.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logspector:
    path: ../../packages/logspector
  logspector_overlay:
    path: ../../overlay

dev_dependencies:
  flutter_test:
    sdk: flutter
